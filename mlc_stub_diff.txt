commit 628cc78028125c7b7d9c04fe5cf6d24cacdd2ca2
Author: El3tar-cmd <abdalrhamn.mahmoud@gmail.com>
Date:   Mon Jun 22 07:16:07 2026 +0300

    Fix MlcEngine: stub ai.mlc.mlcllm refs so build compiles without AAR

diff --git a/app/src/main/java/com/example/MlcEngine.kt b/app/src/main/java/com/example/MlcEngine.kt
index c3a957a..7b2cc44 100644
--- a/app/src/main/java/com/example/MlcEngine.kt
+++ b/app/src/main/java/com/example/MlcEngine.kt
@@ -9,7 +9,9 @@ import java.net.URL
 // ─────────────────────────────────────────────────────────────────────────────
 // MLC LLM Engine
 // Runs entirely in-process via JNI — no external binary, no SELinux issue.
-// Supports Vulkan GPU on Samsung Adreno/Mali automatically (Samsung S20 Ultra ✓)
+// Supports Vulkan GPU on Samsung Adreno/Mali automatically.
+// NOTE: mlc4j AAR not bundled — engine stubs gracefully. Ollama backend is
+//       fully functional for all chat/agent/IDE tasks.
 // ─────────────────────────────────────────────────────────────────────────────
 class MlcEngine(private val context: Context) {
 
@@ -26,6 +28,8 @@ class MlcEngine(private val context: Context) {
             MlcModel("DeepSeek-R1 1.5B (Q4 · ~1.0 GB)","mlc-ai/DeepSeek-R1-Distill-Qwen-1.5B-q4f16_1-MLC","DeepSeek-R1-Distill-Qwen-1.5B-q4f16_1"),
             MlcModel("SmolLM2 1.7B  (Q4 · ~1.1 GB)",  "mlc-ai/SmolLM2-1.7B-Instruct-q4f16_1-MLC",  "SmolLM2-1.7B-Instruct-q4f16_1"),
         )
+
+        private const val MLC_UNAVAILABLE = "MLC LLM library not bundled in this build — use Ollama backend."
     }
 
     // ── Engine state ──────────────────────────────────────────────────────────
@@ -34,9 +38,6 @@ class MlcEngine(private val context: Context) {
     var loadedModelName: String? = null
     val logLines = mutableListOf<String>()
 
-    // mlc4j engine instance — loaded via JNI
-    private var engine: ai.mlc.mlcllm.MLCEngine? = null
-
     val modelsDir: File get() = File(
         context.getExternalFilesDir(null) ?: context.filesDir, "mlc_models"
     ).also { it.mkdirs() }
@@ -50,33 +51,13 @@ class MlcEngine(private val context: Context) {
 
     // ── Load a model into the engine ──────────────────────────────────────────
     fun loadModel(modelDir: File, onLog: (String) -> Unit) {
-        if (isLoading) return
-        Thread {
-            isLoading = true
-            try {
-                onLog("🔄 Initialising MLC LLM…")
-                engine?.unload(); engine = null
-                val eng = ai.mlc.mlcllm.MLCEngine()
-                onLog("📂 Loading: ${modelDir.name}")
-                // reload(modelPath, modelLib) — empty string = auto-detect JIT lib
-                eng.reload(modelDir.absolutePath, "")
-                engine        = eng
-                isLoaded      = true
-                loadedModelName = modelDir.name
-                onLog("✅ Model loaded: ${modelDir.name}")
-                onLog("⚡ Vulkan GPU acceleration active if device supports it")
-            } catch (e: Exception) {
-                isLoaded = false; loadedModelName = null
-                onLog("❌ Load failed: ${e.message}")
-            } finally {
-                isLoading = false
-            }
-        }.start()
+        onLog("⚠️ $MLC_UNAVAILABLE")
+        onLog("💡 Switch to Ollama backend in Settings to use local models.")
     }
 
     fun unloadModel() {
-        engine?.unload(); engine = null
-        isLoaded = false; loadedModelName = null
+        isLoaded = false
+        loadedModelName = null
     }
 
     // ── Chat (blocking — call from a background thread / IO coroutine) ────────
@@ -87,32 +68,7 @@ class MlcEngine(private val context: Context) {
         onToken: (String) -> Unit,
         onComplete: (Boolean, String) -> Unit
     ) {
-        val eng = engine ?: run { onComplete(false, "No model loaded"); return }
-        try {
-            val mlcMsgs = messages.map { msg ->
-                ai.mlc.mlcllm.OpenAIProtocol.ChatCompletionMessage(
-                    role = when (msg.role) {
-                        "assistant" -> ai.mlc.mlcllm.OpenAIProtocol.ChatCompletionRole.assistant
-                        "system"    -> ai.mlc.mlcllm.OpenAIProtocol.ChatCompletionRole.system
-                        else        -> ai.mlc.mlcllm.OpenAIProtocol.ChatCompletionRole.user
-                    },
-                    content = msg.content
-                )
-            }
-            val request = ai.mlc.mlcllm.OpenAIProtocol.ChatCompletionRequest(
-                messages    = mlcMsgs,
-                temperature = temperature,
-                maxTokens   = maxTokens,
-                stream      = true
-            )
-            for (chunk in eng.chat.completions.create(request)) {
-                val token = chunk.choices.firstOrNull()?.delta?.content ?: continue
-                if (token.isNotEmpty()) onToken(token)
-            }
-            onComplete(true, "ok")
-        } catch (e: Exception) {
-            onComplete(false, e.message ?: "inference error")
-        }
+        onComplete(false, MLC_UNAVAILABLE)
     }
 
     // ── Download MLC model from HuggingFace ───────────────────────────────────
@@ -129,7 +85,6 @@ class MlcEngine(private val context: Context) {
                 val destDir = File(modelsDir, model.localName).also { it.mkdirs() }
                 onProgress(0, "Fetching file list from HuggingFace…")
 
-                // 1. List files via HuggingFace API
                 val apiConn = URL("https://huggingface.co/api/models/${model.hfRepo}")
                     .openConnection() as HttpURLConnection
                 apiConn.connectTimeout = 30_000; apiConn.readTimeout = 30_000
